# Odoo upgrade procedure

Copy the database backup to upgrade in this repo under the "db.dump" name  
Update the local OpenUpgrede repo and checkout the branch corresponding to the version you are upgrading to.  

´´´bash
git clone https://github.com/OCA/OpenUpgrade.git
git checkout 11.0
´´´
